## サンプル動画生成用レポジトリ

openAIのttsモデル(gpt-4o-mini-tts)をAPI利用しているのが特徴。

オープンソースではないため実用的でないが、サンプルとしては十分な性能がある

ただし、サンプルとして性能を完全にするには以下の二つを完了する必要がある
1. ttsモデルに渡すテキストを句読点の多いひらがなに統一(gpt-40-mini-ttsはひらがなで音声が安定する)
2. 字幕として渡すテキストは漢字変換をする